# •	nadpisywanie istniejących styli,
# •	tworzenie brakujących styli (create_missing=True),
# •	błąd przy braku rodzica (parent_for_new),
# •	brak modyfikacji oryginalnego stylesheetu (klon),
# •	idempotencję (nie loguje/nie zmienia gdy taka sama wartość),
# •	parsowanie kolorów z #hex,
# •	odrzucanie nieznanych atrybutów,
# •	walidację alignment,
# •	tryb create_missing=False (skip z warningiem).
